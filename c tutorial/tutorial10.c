#include <stdio.h>

int main()
{
    int age;

    printf("enter your age\n");
    scanf("%d",&age);

    //printf("your age is:%d\n",age);

   /* if statement

   if (age>=18)
   {
    printf("eligible for vote");
   }
   */

  /* if-else statement

  if (age>=18)
   {
    printf("eligible for vote");
   }
   else{
    printf("you cannot vote");
   }
   */
     

    return 0;
}
